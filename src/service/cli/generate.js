'use strict';

const {
    getRandomInt,
    shuffle,
    createDate,
} = require(`../../utils`);

const {
    TITLES,
    SENTENCES,
    CATEGORY,
    ArrayElements,
} = require(`../../constants`);

module.exports = {
    name: `--generate`,
    init() {
    }
};